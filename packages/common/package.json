{
  "name": "@apidocs/common",
  "description": "Common code shared between the application and the packages",
  "private": true,
  "license": "Apache-2.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc --build",
    "clean": "rimraf ./dist"
  }
}
